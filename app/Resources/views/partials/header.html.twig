<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href='http://fonts.googleapis.com/css?family=Open+Sans:400,500,600,800' rel='stylesheet' type='text/css'>
    <title>{% block title %}Cours de gratte.com | Retrouvez les meilleurs tutos guitare du net !{% endblock %}</title>
    {% stylesheets 'css/*' filter="cssrewrite"%}
        <link rel="stylesheet" href="{{ asset_url }}" type="text/css"/>
    {% endstylesheets %}
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}"/>
</head>
<body>
<!-- NAVBAR -->
<nav class="navbar navbar-default navbar-static-top">
    <div class="container-fluid">
        <div class="navbar-header">
                <a href="{{ path('main') }}" class="brand-logo">
                    {% image 'images/logo.jpg' %}
                    <img id="logo" src="{{ asset_url }}" alt="logo" />
                    {% endimage %}
                </a>
            <button class="navbar-toggle" data-toggle="collapse" data-target=".navHeaderCollapse">
                {% image 'images/ic_user_new.png' %}
                <img id="userIcon" src="{{ asset_url }}" alt="icon_user" />
                {% endimage %}
            </button>
        </div>
        <div class="collapse navbar-collapse navHeaderCollapse">
            <ul class="nav navbar-nav navbar-right">
                <li><a class="proposition" href="">Poster un TUTO</a></li>

                <!-- Affichage du menu en fonction d'une session ouverte ou non -->
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") == false %}
                <li><a id="inscription-link" href="{{ path('fos_user_registration_register') }}">S'inscrire</a></li>
                <li><a href="{{ path('fos_user_security_login') }}">Se connecter</a></li>
                {% endif %}

                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" id="pseudo_menu" data-toggle="dropdown" role="button" aria-expanded="false">
                        {{ app.user.username }}
                        <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu" role="menu">
                        <!--  admin -->
                        {% if is_granted("ROLE_ADMIN") %}
                            <li><a href="">Administration</a></li>
                        {% endif %}

                        <li><a href="">Mes Favoris</a></li>
                        <li><a href="">Playlist</a></li>
                        <li><a href="">Informations</a></li>

                        <li class="divider"></li>
                        <li><a href="{{ path('fos_user_security_logout') }}">Se déconnecter</a></li>
                    </ul>
                </li>
                {% endif %}
            </ul>
        </div>

    </div>
</nav>

